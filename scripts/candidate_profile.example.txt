Candidate: [Describe yourself in 2-4 sentences: current role/level, education, years and type of relevant experience, key technical skills, languages and fluency level. This is injected directly into the fit-evaluation prompt, so keep it dense and factual — this is what the model uses to judge every posting against.]

Location: [State which locations you're open to, and whether the model should ever downgrade a rating for location — e.g. "Open to all locations; never downgrade a rating for location" or "Only Copenhagen and Aarhus; exclude anything requiring relocation."]

Exclude (No Fit) if: [List the hard exclusion rules specific to you — e.g. seniority levels you don't want, languages you don't speak that are sometimes required, types of roles that aren't a fit regardless of title match, degree/enrollment requirements that don't apply to you.]

Experience-level check: [State your actual years of relevant experience, and give the model a concrete rule for comparing it against a stated requirement — a vague "weigh it seriously" isn't enough to reliably override a strong skills/title match. Something like: "if my experience is less than ~75% of the posting's stated minimum, rate it No Fit; only use Borderline if I'm close to the minimum." This was a real bug: a posting requiring "3-6 years" got rated Strong Fit for a candidate with under 2 years, because nothing told the model to check — and a soft instruction alone only produced a Borderline rating instead of the No Fit the candidate actually wanted for that size of gap.]

Incomplete-description check: [Keep this instruction — don't remove it. Some portals only scrape a short teaser instead of the full posting (confirmed real case: 600 characters, two intro paragraphs, no requirements section — the actual posting required "3+ years" experience twice, invisible in what was fetched). Tell the model that a description with no visible requirements/qualifications list is not evidence none exist, and to cap the rating rather than assume a full match.]
